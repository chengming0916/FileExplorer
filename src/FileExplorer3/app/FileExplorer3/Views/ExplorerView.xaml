<UserControl x:Class="FileExplorer.Views.ExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <ContentControl Grid.Column="0"  Name="DirectoryTreeModel" Margin="2" />
            <ContentControl Grid.Column="1"  Name="FileListModel" Margin="2" />
            
            <ContentControl Grid.ColumnSpan="2" Grid.Row="1"  Name="StatusbarModel" />
            <GridSplitter Grid.Row="1" Grid.ColumnSpan="2" ResizeDirection="Rows" Height="3" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
        </Grid>
    </Grid>
</UserControl>
