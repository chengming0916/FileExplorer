<UserControl x:Class="FileExplorer.Views.FileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:uc="clr-namespace:FileExplorer.UserControls"
             xmlns:m="clr-namespace:FileExplorer.Models"
             mc:Ignorable="d" 
             Height="300"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/FileExplorer3;component/Themes/Default/Brushes.xaml" />
                <ResourceDictionary Source="pack://application:,,,/FileExplorer3;component/Themes/Common/FileListSupp.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!--<DataTemplate x:Key="DefaultEntryTemplate">
                <TextBlock Text="{Binding EntryModel.Label}"></TextBlock>
            </DataTemplate>-->



        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" MinHeight="150" />
        </Grid.RowDefinitions>
        <Button x:Name="Load" cal:View.Context="{Binding Items}" Grid.Row="0">Load</Button>
        <ListView Grid.Row="1" x:Name="Items"  Background="LightBlue">
            <!--<ListView.ItemTemplate>                    
                    <DataTemplate>
                        <TextBlock Text="{Binding EntryModel.Label}"></TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>-->

        </ListView>

        <!--<TextBlock x:Name="BusyIndicator" Grid.Row="1">Busy</TextBlock>-->
    </Grid>
</UserControl>
