<UserControl x:Class="FileExplorer.Views.FileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:uc="clr-namespace:FileExplorer.UserControls"
             xmlns:m="clr-namespace:FileExplorer.Models"
             mc:Ignorable="d" 
             Height="300"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/FileExplorer3;component/Themes/Default/Brushes.xaml" />
                <!--
                <ResourceDictionary Source="pack://application:,,,/FileExplorer3;component/Themes/Common/ItemTemplates.xaml" />
                <ResourceDictionary Source="pack://application:,,,/FileExplorer3;component/Themes/Common/Panels.xaml" />
                -->
                <ResourceDictionary Source="pack://application:,,,/FileExplorer3;component/Themes/Common/FileList.xaml" />
                <ResourceDictionary Source="pack://application:,,,/FileExplorer3;component/Themes/Common/Views.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!--<DataTemplate x:Key="DefaultEntryTemplate">
                <TextBlock Text="{Binding EntryModel.Label}"></TextBlock>
            </DataTemplate>-->



        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>

        <uc:FileList Background="LightBlue"  x:Name="ProcessedItems"                      
                     ItemsSource="{Binding Items}" 
                     ViewMode="{Binding ViewMode}"  
                     Columns="{Binding ColumnList}"
                     ColumnsVisibility="Visible"
                     ItemSize="{Binding ItemSize}"
                     ItemAnimationDuration="00:00:01"
                     SortDirection="{Binding SortDirection}"
                     SortBy="{Binding SortBy}"
                     SelectionModeEx="SelectionHelper"
                     Style="{StaticResource FileListStyle}"         
                     cal:View.ApplyConventions="False"
                     cal:Message.Attach = "[Event SelectionChangedEvent] = [Action OnSelectionChanged($this.SelectedItems)]">

            <i:Interaction.Triggers>
                <uc:InputBindingTrigger>
                    <uc:InputBindingTrigger.InputBinding>
                        <KeyBinding Key="F2"/>
                    </uc:InputBindingTrigger.InputBinding>
                    <cal:ActionMessage MethodName="Rename"/>
                </uc:InputBindingTrigger>
            </i:Interaction.Triggers>
            <!--<uc:FileList.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path=Name}"/>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </uc:FileList.GroupStyle>-->


        </uc:FileList>
    </Grid>
</UserControl>
